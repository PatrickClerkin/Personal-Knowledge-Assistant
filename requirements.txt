# ── Core Dependencies ────────────────────────────────────────────────
# Document parsing
PyMuPDF>=1.23.0
python-docx>=1.0.0

# Embeddings and vector search
# NOTE: sentence-transformers pinned to 3.1.1 — newer versions introduced
# a meta-tensor loading regression that fails with torch 2.5+
sentence-transformers==3.1.1
faiss-cpu>=1.7.0
numpy>=1.24.0

# NLP
nltk>=3.8.0
networkx>=3.0
spacy>=3.7.0
# After installing, run:  python -m spacy download en_core_web_sm

# Web framework
flask>=3.0
Jinja2>=3.1.0

# Configuration
PyYAML>=6.0
pydantic>=2.0
python-dotenv>=1.0.0

# CLI
click>=8.0

# BM25 keyword search
rank-bm25>=0.2.2

# ── Optional: RAG (LLM-powered features) ────────────────────────────
anthropic>=0.25.0

# ── Optional: Advanced semantic chunking ─────────────────────────────
# hdbscan>=0.8.33
# umap-learn>=0.5.3
# bertopic>=0.16.0
scikit-learn>=1.0

# ── Optional: Cross-encoder reranking ────────────────────────────────
# (uses sentence-transformers CrossEncoder — already installed above)

# ── Development ──────────────────────────────────────────────────────
pytest>=7.0
black>=23.0
flake8>=6.0

# ── Transitive dependencies (pinned for reproducibility) ────────────
# torch pinned to 2.5.x — torch 2.6+ triggers meta-tensor loading bug
# with sentence-transformers when moving models between devices
torch==2.5.1
sympy==1.13.1
certifi==2025.11.12
charset-normalizer==3.4.4
colorama==0.4.6
filelock==3.20.0
fsspec==2025.12.0
huggingface-hub==0.36.0
idna==3.11
iniconfig==2.3.0
joblib==1.5.2
lxml==6.0.2
MarkupSafe==3.0.3
mpmath==1.3.0
packaging==25.0
pluggy==1.6.0
regex==2025.11.3
requests==2.32.5
safetensors==0.7.0
scipy==1.16.3
setuptools>=68.0
threadpoolctl==3.6.0
tokenizers==0.22.1
tqdm>=4.60.0
transformers>=4.41.0
typing_extensions>=4.5.0
urllib3>=2.0.0
werkzeug>=3.0.0